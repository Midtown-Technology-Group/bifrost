# Snyk dependency, IaC, and container scans.
#
# Snyk is a blocking security signal. A missing token, scanner failure, or
# high-severity finding must not produce a green audit job.

name: Snyk

on:
  pull_request:
    branches: [main]
    paths:
      - "pyproject.toml"
      - "requirements*.lock"
      - "client/package.json"
      - "client/package-lock.json"
      - "api/Dockerfile"
      - "client/Dockerfile"
      - "k8s/**"
      - ".snyk"
      - ".github/workflows/snyk.yml"
  schedule:
    - cron: "0 11 * * 1"
  workflow_dispatch:

permissions:
  contents: read

env:
  SNYK_ORG: ${{ vars.SNYK_ORG }}
  SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

jobs:
  open-source:
    name: Open Source
    runs-on: ubuntu-latest
    steps:
      - name: Require Snyk token
        if: env.SNYK_TOKEN == ''
        run: |
          echo "SNYK_TOKEN is required; refusing to report a green audit without scanning." >&2
          exit 1

      - name: Verify Snyk authentication
        if: env.SNYK_TOKEN != ''
        run: |
          response_file="$(mktemp)"
          status="$(curl -sS -o "$response_file" -w "%{http_code}" \
            -H "Authorization: token $SNYK_TOKEN" \
            "https://api.snyk.io/rest/orgs?version=2024-10-15&limit=100")"
          if [ "$status" != "200" ]; then
            echo "Snyk authentication failed (HTTP $status); refusing to scan without valid credentials." >&2
            exit 1
          fi
          if [ -n "$SNYK_ORG" ] && ! grep -Eq "\"slug\": ?\"$SNYK_ORG\"" "$response_file"; then
            echo "Snyk token is valid but organization '$SNYK_ORG' is not visible to it; check SNYK_ORG and token access." >&2
            exit 1
          fi

      - name: Checkout
        if: env.SNYK_TOKEN != ''
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node
        if: env.SNYK_TOKEN != ''
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "20"

      - name: Set up Python
        if: env.SNYK_TOKEN != ''
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"

      - name: Prepare Python dependency manifest for Snyk
        id: python-requirements
        if: env.SNYK_TOKEN != ''
        run: |
          python - <<'PY'
          from pathlib import Path
          import os

          source = Path("requirements.lock")
          target = Path(os.environ["RUNNER_TEMP"]) / "bifrost-snyk-requirements.txt"
          lines = []
          for line in source.read_text(encoding="utf-8").splitlines():
              stripped = line.strip()
              if not stripped or stripped.startswith("#") or stripped.startswith("--hash="):
                  continue
              if "==" in stripped:
                  lines.append(stripped.split(" \\")[0])
          target.write_text("\n".join(lines) + "\n", encoding="utf-8")
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output:
              print(f"requirements_file={target}", file=output)
          PY

      - name: Install Python dependencies
        if: env.SNYK_TOKEN != ''
        run: |
          python -m venv .venv
          ./.venv/bin/pip install --require-hashes -r requirements.lock

      - name: Test Python dependencies
        if: env.SNYK_TOKEN != ''
        run: npx --yes snyk@1.1304.3 test --file="${{ steps.python-requirements.outputs.requirements_file }}" --package-manager=pip --policy-path=.snyk --severity-threshold=high --skip-unresolved=true --command=./.venv/bin/python ${SNYK_ORG:+--org="$SNYK_ORG"}

      - name: Test client dependencies
        if: env.SNYK_TOKEN != ''
        run: npx --yes snyk@1.1304.3 test --file=client/package-lock.json --package-manager=npm --severity-threshold=high ${SNYK_ORG:+--org="$SNYK_ORG"}

      - name: Monitor default branch snapshots
        if: always() && env.SNYK_TOKEN != '' && github.event_name != 'pull_request'
        run: |
          npx --yes snyk@1.1304.3 monitor --file="${{ steps.python-requirements.outputs.requirements_file }}" --package-manager=pip --policy-path=.snyk --project-name=bifrost-api --skip-unresolved=true --command=./.venv/bin/python ${SNYK_ORG:+--org="$SNYK_ORG"}
          npx --yes snyk@1.1304.3 monitor --file=client/package-lock.json --package-manager=npm --project-name=bifrost-client ${SNYK_ORG:+--org="$SNYK_ORG"}

  iac:
    name: IaC
    runs-on: ubuntu-latest
    steps:
      - name: Require Snyk token
        if: env.SNYK_TOKEN == ''
        run: |
          echo "SNYK_TOKEN is required; refusing to report a green audit without scanning." >&2
          exit 1

      - name: Verify Snyk authentication
        if: env.SNYK_TOKEN != ''
        run: |
          response_file="$(mktemp)"
          status="$(curl -sS -o "$response_file" -w "%{http_code}" \
            -H "Authorization: token $SNYK_TOKEN" \
            "https://api.snyk.io/rest/orgs?version=2024-10-15&limit=100")"
          if [ "$status" != "200" ]; then
            echo "Snyk authentication failed (HTTP $status); refusing to scan without valid credentials." >&2
            exit 1
          fi
          if [ -n "$SNYK_ORG" ] && ! grep -Eq "\"slug\": ?\"$SNYK_ORG\"" "$response_file"; then
            echo "Snyk token is valid but organization '$SNYK_ORG' is not visible to it; check SNYK_ORG and token access." >&2
            exit 1
          fi

      - name: Checkout
        if: env.SNYK_TOKEN != ''
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node
        if: env.SNYK_TOKEN != ''
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "20"

      - name: Test Kubernetes manifests
        if: env.SNYK_TOKEN != ''
        run: npx --yes snyk@1.1304.3 iac test k8s --severity-threshold=high ${SNYK_ORG:+--org="$SNYK_ORG"}

  containers:
    name: Containers
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    steps:
      - name: Require Snyk token
        if: env.SNYK_TOKEN == ''
        run: |
          echo "SNYK_TOKEN is required; refusing to report a green audit without scanning." >&2
          exit 1

      - name: Checkout
        if: env.SNYK_TOKEN != ''
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node
        if: env.SNYK_TOKEN != ''
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "20"

      - name: Test published API image
        if: env.SNYK_TOKEN != ''
        run: npx --yes snyk@1.1304.3 container test ghcr.io/midtown-technology-group/bifrost-api:dev --file=api/Dockerfile --severity-threshold=high ${SNYK_ORG:+--org="$SNYK_ORG"}

      - name: Test published client image
        if: env.SNYK_TOKEN != ''
        run: npx --yes snyk@1.1304.3 container test ghcr.io/midtown-technology-group/bifrost-client:dev --file=client/Dockerfile --severity-threshold=high ${SNYK_ORG:+--org="$SNYK_ORG"}
